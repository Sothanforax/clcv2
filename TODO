Figure out linker options and whether or not it'll be dynamic or static
Switch to getopt
Ncurses tui
Simple prompt
Regex for argv input from command line
Regex for the prompt and tui as well
Write a makefile and maybe a configure script (if need be)
